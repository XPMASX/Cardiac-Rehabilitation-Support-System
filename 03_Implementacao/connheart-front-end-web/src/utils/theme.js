// utils/theme.js
import { createTheme } from '@mui/material/styles';

// Create a theme instance.
const themePaciente = createTheme({
    palette: {
        primary: {
            light: '#599FCC'
        }
    }
});

export default themePaciente;
